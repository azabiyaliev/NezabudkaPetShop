import { Box, Typography } from '@mui/material';

const Filters = () => {
  return (
    <Box>
      <Typography variant="h5" sx={{ textAlign: "center" }}>Здесь будут фильтры</Typography>
    </Box>
  );
};

export default Filters;